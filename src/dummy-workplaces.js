const wp = {
  id,
  name,
  code
};

// create service object to query db table
// workplaces where code is equal to input code

// create variable isUnique set to false
// create variable to hold code

while (!isUnique) {
  // generate code
  // query db to see if code already exists
  // if it does not exist in db
  // set isUnique to true
}

// set workplace code to the last code generated
